# Functions
> Sytax for defining functions

# Void Function
```
fn print_sum(num1, num2) {
    put(num1 + num2);
}
```

# Function with return
```
fn sum(num1, num2) -> sum {
    sum = num1 + num2;
}
```
